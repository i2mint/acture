# Superseded

The Phase-1-bound handoff content has been promoted to [`next_session.md`](next_session.md), which is now the canonical rotating "what to do next session" prompt. New convention going forward:

- `docs/next_session.md` — the active handover prompt; rotated each phase.
- Per-phase reflection (`docs/phase-N-reflection.md`) — the archive of what was learned.

This file is retained as a redirect; it can be deleted whenever convenient.
